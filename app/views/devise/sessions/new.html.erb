<div class="container row mx-auto">
  <div class="signin text-center py-5 px-3 col-md-6 mx-auto rounded">
    <h3 class="signin-title mb-5">ログイン</h3>
    <h5 class="mb-3">会員の方はこちらからログイン</h5>
    <%= form_with model: @user, url: user_session_path, class: "col-12", local: true do |f| %>

      <div class="field mb-4">
        <i class="far fa-envelope mr-2"></i><%= f.label "メールアドレス" %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", size: "50" %>
      </div>
      <div class="field mb-5">
        <i class="fas fa-key mr-2"></i><%= f.label "パスワード" %><br />
        <%= f.password_field :password, autocomplete: "current-password", size: "50" %>
      </div>
      <%= f.submit "ログイン", class: "btn w-75 btn-primary"%>
    <% end %>
    <div class="mt-5">
      <h5>会員登録がお済みでない方</h5>
      <%= link_to "こちら", new_user_registration_path %>　から会員登録を行ってください。
    </div>
  </div>
</div>